function btnReauthorize_direct() {     // тоже можно повесить на кнопку
  try {
    ScriptApp.invalidateAuth();
    DriveApp.getRootFolder();          // здесь обычно и падает без авторизации
    SpreadsheetApp.getActive().toast('Доступ сброшен. Запусти нужную команду — появится окно разрешений.', 'Переавторизация', 5);
  } catch (e) {
    SpreadsheetApp.getUi().alert('Требуется авторизация. Откройте меню «🔐 Доступ → Переавторизовать…» или Extensions → Apps Script и запустите reauthorizeForUser_.');
  }
}
